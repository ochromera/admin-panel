import React from 'react';
import {Box, Typography } from "@mui/material";

const RSidebar = () => {
    return (
        <Box>
            <Typography>
                RSidebar
            </Typography>
        </Box>
    );
};

export default RSidebar;
