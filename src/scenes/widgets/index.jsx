import React from 'react';
import {Box} from "@mui/material";
import Header from "../../components/header/Header";

const Widgets = () => {
    return (
        <Box>
            <Header title='Welcome to Widgets' subtitle=""/>
        </Box>
    );
};

export default Widgets;
